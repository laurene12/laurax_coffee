import 'package:flutter/material.dart';

class SelectedOption extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: const Text('New Screen')),
        body: Center(
          child: Text("Ici c'est congo"),
        ));
  }
}
